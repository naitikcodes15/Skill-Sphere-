rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // For development, allowing all reads and writes
      // In production, this should be restricted to authenticated users
      allow read, write: if true;
    }
  }
}
